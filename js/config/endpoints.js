const BASE_URL = 'http://localhost';

const GET_BROTHS = `${BASE_URL}/broths`;
const GET_PROTEINS = `${BASE_URL}/proteins`;
const POST_ORDERS = `${BASE_URL}/orders`;

export { GET_BROTHS, GET_PROTEINS, POST_ORDERS };
